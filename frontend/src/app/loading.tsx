import { LoadingScreen } from "@/components/shared/loading-screen";

/** Global route-level loading screen (CONTRACT §11.7). */
export default function Loading() {
  return <LoadingScreen fullscreen />;
}
